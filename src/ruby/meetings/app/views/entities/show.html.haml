%p#notice= notice

%p
  %b Name:
  = @entity.name
%p
  %b Wikipedia entry:
  = @entity.wikipedia_entry

%table.table.table-inverse.table-striped
  %thead
    %tr
      %th Month
      %th Year
      %th Purpose
      %th Organisations

  %tbody
    - @entity.meetings.each do |meeting|
      %tr
        %td= meeting.month
        %td= meeting.year
        %td= meeting.purpose
        %td= meeting.organisations.map(&:name).join(', ')

%br
= link_to 'Edit', edit_entity_path(@entity)
\|
= link_to 'Back', entities_path
